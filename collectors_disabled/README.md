# Disabled collectors

This directory is an optional parking area for collector scripts you do not want
`pipeline/00_run_all.sh` to execute.

Collectors are only executed from the `collectors/` directory.
